import { HighlightNumbersPipe } from './highlight-numbers.pipe';

describe('HighlightNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
